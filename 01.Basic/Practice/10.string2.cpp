// Write a program that takes a string input.
// Check if the string is a palindrome (reads the same forwards and backward, ignoring case and spaces for a more advanced version, but start simple). 
// Print "Palindrome" or "Not a Palindrome".

#include<iostream>
using namespace std;

int main(){
    string str;
    cout << "Enter the string." << endl;
    cin >> str;

    
}

